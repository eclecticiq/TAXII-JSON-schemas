{
    "$schema": "http://json-schema.org/draft-04/schema",

    "title": "Schema for a Service Instance",
    "type": "object",

    "definitions": {
        "query-format": {
            "$ref": "file:query-format.json"
        },
        "content-binding": {
            "$ref": "file:content-binding.json"
        }
    },

    "required": ["address", "service_type", "service_version", "protocol_binding", "message_bindings"],

    "properties": {
        "service_type": {
            "enum" : ["DISCOVERY", "COLLECTION_MANAGEMENT", "INBOX", "POLL"]
        },
        "available": {
            "type": "boolean"
        },
        "address": {
            "type": "string",
            "format": "uri"
        },
        "service_version": {
            "type": "string"
        },
        "protocol_binding": {
            "type": "string"
        },
        "message_bindings": {
            "type": "array",
            "items": {
                "type": "string"
            },
            "minItems": 1,
            "uniqueItems": true
        },
        "supported_queries": {
            "$ref": "#/definitions/query-format"
        },
        "message": {
            "type": "string"
        }
    }
}

